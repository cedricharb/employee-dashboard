// This is the employee type, put it here so it can be easily reusable

export type Employee = {
  id: string;
  name: string;
  title: string;
  department: string;
  location: string;
  picture: string;
};
